import fs from 'node:fs/promises';
import path from 'node:path';
const ROOT=path.join(process.cwd(),'legacy-pages');
const decode=(v='')=>v.replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>');
function attr(tag,name){const m=tag.match(new RegExp(`${name}\\s*=\\s*["']([^"']*)["']`,'i'));return m?decode(m[1]):''}
function candidates(parts=[]){const raw=parts.map(decodeURIComponent).join('/');if(raw.includes('..')||raw.includes('\\')||raw.includes('\0'))return[];if(!raw)return['index.html'];const s=raw.replace(/^\/+|\/+$/g,'');return s.endsWith('.html')?[s]:[`${s}.html`,`${s}/index.html`]}
export async function loadLegacyPage(parts=[]){for(const rel of candidates(parts)){const file=path.resolve(ROOT,rel);if(!file.startsWith(path.resolve(ROOT)+path.sep))continue;try{const raw=await fs.readFile(file,'utf8');return parse(raw.replace(/\r\n/g,'\n').replace(/\r/g,'\n'),rel)}catch(e){if(e?.code!=='ENOENT')throw e}}return null}
function parse(html,sourcePath){const head=html.match(/<head[^>]*>([\s\S]*?)<\/head>/i)?.[1]||'';let body=html.match(/<body[^>]*>([\s\S]*?)<\/body>/i)?.[1]||html;const bodyAttrs=html.match(/<body([^>]*)>/i)?.[1]||'';const stylesheets=[...head.matchAll(/<link\b[^>]*rel=["'][^"']*stylesheet[^"']*["'][^>]*>/gi)].map(x=>({href:attr(x[0],'href'),media:attr(x[0],'media')||undefined})).filter(x=>x.href);const inlineStyles=[...head.matchAll(/<style\b[^>]*>([\s\S]*?)<\/style>/gi)].map(x=>x[1]);const scripts=[];const collect=m=>m.replace(/<script\b([^>]*)>([\s\S]*?)<\/script>/gi,(_,a,code)=>{const tag=`<script ${a}>`;scripts.push({src:attr(tag,'src')||null,type:attr(tag,'type')||null,async:/\basync\b/i.test(a),defer:/\bdefer\b/i.test(a),code:code||''});return''});collect(head);body=collect(body);const title=decode(head.match(/<title[^>]*>([\s\S]*?)<\/title>/i)?.[1]?.trim()||'AARAA Infrastructure');const mt=head.match(/<meta\b[^>]*name=["']description["'][^>]*>/i)?.[0]||'';const ct=head.match(/<link\b[^>]*rel=["']canonical["'][^>]*>/i)?.[0]||'';return{sourcePath,title,description:attr(mt,'content'),canonical:attr(ct,'href'),bodyClass:attr(`<body ${bodyAttrs}>`,'class'),body,stylesheets,inlineStyles,scripts}}
