export const metadata = {
  title: 'HR Admin Dashboard | AARAA Infrastructure',
  robots: {
    index: false,
    follow: false
  }
};

export default function AdminLayout({ children }) {
  return children;
}
